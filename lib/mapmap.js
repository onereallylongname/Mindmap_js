function Maptree(version, id, type = "generic") {
  return {
    info: {
      versionn
      id,                 // self id, base64
      type,
      date: Date.now(),   // node creation date
    },
    trees: {},
  }
}
